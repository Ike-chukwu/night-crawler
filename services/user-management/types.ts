export type User = {
    id: string;
    userMail: string;
    country: string;
    phoneNumber: string;
    userRole: string;
};

export type Event = {
    id: string;
    eventName: string,
    country: string,
    eventLocation: string,
    date: string,
};